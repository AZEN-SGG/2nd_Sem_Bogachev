#ifndef SOLVE
#define SOLVE

#include <string.h>
#include <stdbool.h>

#include "array.h"

int t8_solve(char **arr, int n, char *s);
bool is_inside(char *a, char *s);
int ccmp(char a, char s);

#endif
